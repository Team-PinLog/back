-- 설정 화면이 이메일을 반드시 표시해야 하므로 값 없는 계정을 두지 않는다(06 §2.2).
--
-- 백필을 넣지 않는 이유: 이 컬럼이 NULL인 행은 운영에 없다. 있었다면 채울 값이 없다 —
-- 이메일은 공급자가 주는 값이고 서버가 만들어 낼 수 있는 것이 아니다. 그런 행이 있는
-- 환경에서는 이 마이그레이션이 실패하는 편이 맞다. 무엇으로 채울지는 데이터를 보고
-- 사람이 정할 문제이고, 여기서 임의 값을 넣으면 "표시할 이메일"이라는 목적이 깨진다.
--
-- 탈퇴 시 마스킹은 이 제약과 부딪히지 않는다. 마스킹은 치환이며 NULL로 만드는 것이
-- 아니다(06 §2.2). 같은 테이블의 provider_user_id가 이미 NOT NULL이면서 마스킹 대상이다.
ALTER TABLE core.social_account
    ALTER COLUMN email SET NOT NULL;
