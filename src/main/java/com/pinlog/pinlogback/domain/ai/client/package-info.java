/**
 * FastAPI AI Server 내부 API 클라이언트.
 *
 * <p>{@link org.jspecify.annotations.NullMarked}로 선언한다(BD-29과 같은 이유·같은 단위).
 * {@code placeMeta}처럼 <b>없는 것이 정상</b>인 값이 있어 그 사실이 타입에 드러나야 한다.
 */
@NullMarked
package com.pinlog.pinlogback.domain.ai.client;

import org.jspecify.annotations.NullMarked;
