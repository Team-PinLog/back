/**
 * 로그인 진입·재발급·로그아웃 엔드포인트.
 *
 * <p>{@link org.jspecify.annotations.NullMarked}로 선언한다(BD-27과 같은 이유·같은 단위).
 * {@code @CookieValue(required = false)}로 받는 값이 nullable이므로 표기가 필요하다.
 */
@NullMarked
package com.pinlog.pinlogback.domain.auth.controller;

import org.jspecify.annotations.NullMarked;
